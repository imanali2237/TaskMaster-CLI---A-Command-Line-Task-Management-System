export enum Command {
    
    ADD = "add",
    LIST = "list",
    COMPLETE = "complete",
    DELETE = "delete",
    EXPORT = "export",
    IMPORT = "import",
    STATS = "stats",
    WATCH = "watch",
}